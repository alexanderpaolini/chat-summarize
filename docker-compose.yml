services:
  chat-summarize:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: chat-summarize-bot
    restart: unless-stopped
    env_file:
      - path: .env
        required: true
